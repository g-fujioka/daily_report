<% provide(:title, t('title.department_update')) %>
<h1><%= t('title.department_update') %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@department) do |f| %>
        <%= render 'form', {f: f} %>
    <% end %>
  </div>
</div>