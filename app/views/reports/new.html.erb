<% provide(:title, t('title.new_report')) %>
<h1><%= t('title.new_report') %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@report) do |f| %>
        <%= render 'form', {f: f} %>
    <% end %>
  </div>
</div>