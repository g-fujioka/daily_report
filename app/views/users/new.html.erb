<% provide(:title, t('title.new_user')) %>
<h1><%= t('title.new_user') %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user, url: users_path) do |f| %>
      <%= render 'form', {f: f} %>
    <% end %>
  </div>
</div>

